import React from 'react';

const Login = () => {
  return (
    <form onSubmit={}>
      <input name="username" type="text" onChange={} value={}/>
      <input name="password" type="password" onChange={} value={}/>
      <input type="submit" value="Log In"/>
    </form>
  );
}

export default Login;


